.TH _PRINTF 3 "October 2, 2025" "Holberton School" "User Manual"
.SH NAME
_printf \- custom implementation of the printf function

.SH SYNOPSIS
.B #include "main.h"

.B int _printf(const char *format, ...);

.SH DESCRIPTION
The
.B _printf()
function sends formatted output to the standard output (stdout), just like the standard
.B printf()
function in C.

This custom version supports a limited set of format specifiers:

.SH SUPPORTED FORMAT SPECIFIERS

.TP
.B %c
Prints a single character.

.TP
.B %s
Prints a string.

.TP
.B %%
Prints a percent sign.

.TP
.B %d, %i
Prints a signed decimal integer.

.SH RETURN VALUE
Returns the number of characters printed (excluding the null byte).

Returns a negative value if an error occurs.

.SH EXAMPLE
.EX
_printf("Hello %s, number: %d%%\n", "World", 100);
.EX
Output: Hello World, number: 100%

.SH NOTES
- This is a simplified version of the standard printf.
- It does not handle flags, field width, precision, or length modifiers.

.SH AUTHORS
Created by students at Holberton School.

.SH SEE ALSO
.BR printf (3),
.BR write (2),
.BR va_start (3)
